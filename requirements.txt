# ── LangChain core ────────────────────────────────────────────────
# LƯU Ý: RAGAS 0.4.x vẫn import langchain_community.chat_models.vertexai,
# module này đã bị xoá trong LangChain 1.x → phải ghim thế hệ 0.3.x.
langchain>=0.3.27,<0.4
langchain-core>=0.3.80,<0.4
langchain-community>=0.3.31,<0.4
langchain-text-splitters>=0.3.11,<0.4

# ── LLM Providers (cài tất cả để hỗ trợ đa provider) ─────────────
langchain-openai>=0.3.35,<0.4         # OpenAI + OpenRouter + DeepSeek
langchain-anthropic>=0.3.22,<0.4      # Anthropic Claude
langchain-google-genai>=2.1.12,<3     # Google Gemini
langchain-ollama>=0.2.3,<0.3          # Ollama (local)

# ── LangSmith ─────────────────────────────────────────────────────
langsmith>=0.4.0,<0.5

# ── Vector store ──────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────
ragas>=0.4.0,<0.5
datasets>=2.0.0                  # required by RAGAS internally
numpy>=1.25.0

# ── Guardrails AI ─────────────────────────────────────────────────
guardrails-ai>=0.5.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=1.0.0                    # OpenAI SDK (used by langchain-openai)
tiktoken>=0.5.0                  # token counting
requests>=2.31.0                 # gọi REST API liệt kê model của provider
